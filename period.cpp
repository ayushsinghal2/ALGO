#include <iostream>
#include <fstream>
#include <ctime>
using namespace std;
void main()
{
	fstream o;
	o.open("period.bin",ios::out | ios::app | ios::binary);
	char arr[1000];
	for (int i = 0; i < 100; ++i) ////can be modified for all numbers...
	{
		
	}
}